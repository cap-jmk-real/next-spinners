# Match Prettier `endOfLine: "lf"` so Windows checkouts do not flip CI to CRLF.
* text=auto eol=lf

# Binary assets — never apply text normalization
*.gif binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
